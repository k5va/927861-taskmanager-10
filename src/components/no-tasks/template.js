/**
 * Generates HTML template for no-tasks component
 * @return {String} - template
 */
const template = () => {
  return (
    `<p class="board__no-tasks">
      Click «ADD NEW TASK» in menu to create your first task
    </p>`
  );
};

export {template};
